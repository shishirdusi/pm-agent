# Core (needed for the demo as shipped)
# - stdlib only: csv, json, difflib, re, argparse, dataclasses

# Loads ANTHROPIC_API_KEY (and friends) from a .env file automatically, so you don't
# have to `export` it in every new terminal session. Fully optional - `export` still
# works fine without this installed.
python-dotenv>=1.0.0

# Real LLM extraction path (used automatically when ANTHROPIC_API_KEY is set)
anthropic>=0.121.0

# Local web UI (server.py) - a thin HTTP layer over the same extractor/tracker/review
# modules the CLI uses, so the browser never reimplements any logic.
fastapi>=0.110.0
uvicorn>=0.29.0

# Optional: Slack bot integration (see server.py's "Slack integration" section
# and README.md for setup). Only used if SLACK_BOT_TOKEN/SLACK_SIGNING_SECRET
# are set - safe to leave installed even if you never use it.
slack_sdk>=3.27.0

# Optional: live Google Sheets writes (see sheets_writer.py for setup)
gspread>=6.0.0
google-auth>=2.30.0

# Dev only - for running tests/
pytest>=8.0.0
